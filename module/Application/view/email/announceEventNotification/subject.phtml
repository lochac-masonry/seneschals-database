<?php
declare(strict_types=1);

// Stay in PHP-mode to ensure result is one line.
print(
    $hostGroup['groupname'] . ', ' .
    $this->formatDate($values['startdate'], $values['enddate']) . ': ' .
    $values['name']
);
