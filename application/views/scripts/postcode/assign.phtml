<p>Use this page to assign a block of postcodes to a group.<br />
Select a numerical range of postcodes (or the same postcode twice to assign a single postcode), and select a recipient group, then click 'Submit' to commit the changes.</p>

<?php
echo $this->form;
?>
